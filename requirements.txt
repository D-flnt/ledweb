# LedWeb Dependencies
# Installeer met: pip3 install -r requirements.txt
# Of voor Python 3.13+: pip3 install --break-system-packages -r requirements.txt

flask>=2.3.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
